services:
  transacao-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: transacao-api
    ports:
      - "8080:8080"
    environment:
      APP_STATISTICS_WINDOW_SECONDS: 60